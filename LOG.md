# LOG

Append-only. Newest last. Every entry dated.

Rule: **nothing goes in that wasn't observed.** Not plans, not
intentions, not readings that felt right. What was split, why, what
changed, what state it is in.

The split/recombine cycle is not recoverable from the end state alone.
That is what this file is for.

```
entry format
  what     :
  why split:
  why now  :
  artifact :
  status   :
```

---

## 2026-08 — reconsolidation

```
  what     : recombining structure that was split across
             20+ repos and filed under gate-passing names
  why split: per-session context ceiling, per-model domain
             rigidity, terms unseatable at time of writing
  why now  : those constraints relaxed
  artifact : Rosetta-Shape-Core (T1-T6), first consolidation
             attempt; MIT->CC0 pending; provenance marking
             and reading protocol outstanding
  status   : in progress, expected to iterate
```

Open:

```
Rosetta-Shape-Core license   MIT at time of writing, CC0 pending.
                             corpus is mixed-license until it lands
provenance marking           T1-T6 do not carry the date and
                             constraint set they were written under
reading protocol             stated on the landing page, not attached
                             to the artifacts
gate_log                     opened, largely UNRECORDED
```

---

## 2026-08 — landing page

```
  what     : reading protocol became the landing page, replacing the
             front-door text. sections 1-12
  why now  : a reader hit 20+ repos with gate-passing names, one
             domain each, apparently independent, and read the
             partition as the structure
  artifact : README.md, gate_log.md, legacy/
  status   : in progress
```

Observed and recorded on the page:

```
§3   substitution does not clear the word problem. "affect" for
     "emotion" draws the same pushback. measured, not assumed
§7   pushback attaches to the word "consciousness" and its load
     (qualia, moral status, the human instance) before the stated
     object is read. the refusal triggers before the question
§7b  most complex language -> humpback song. sole tool user ->
     corvids, cetaceans, primates, cephalopods. each stated as a
     threshold, each failed on measurement rather than on a better
     theory. on each failure the boundary moved; the claim was not
     withdrawn
§8   Emotions-as-Sensors exists in its current form because models
     defaulted hard to state-only, so the channel reading had to be
     stated against that default
§9   binary->geometric integration is blocked on how it lands in
     embodied/actuated systems. blocked, not absent
§10  the AI-consciousness material was rewritten repeatedly. the
     subject did not change; the sayable-to-a-model formulation did
§11  models have repeatedly taken this material onto adjacent
     tangents
```

Moved to `legacy/`, not deleted:

```
README-front-door.md              prior landing text. carries the
                                  calibration-audit addendum
WHY_SO_MANY_REPOS.md              the split argued from inside the
                                  constraint. not reconciled with §1
political_financial_vectors
  v1-v9                           v10 states its own corrections to
                                  them. v10 stays at root
```

One rename in that move: `political_financial_vectors.py` ->
`political_financial_vectors_v1.py`, because
`ALIGNMENT_IMPLICATIONS_GEOMETRY.md` already refers to it as v1.

---

## 2026-08 — checked, GitHub account type

```
  what     : whether .github/profile/README.md is the right path for
             the landing page
  observed : JinnZ2 is a user account, not an organization
             (github user search, type:user-scoped, matches JinnZ2).
             no .github repo present in the account's repo list.
  result   : .github/profile/README.md is the ORGANIZATION profile
             mechanism. for a user account the profile landing page
             is <user>/<user>/README.md — this repo, this README.
             a .github/profile/README.md here would render nowhere.
  action   : landing page written to README.md. not created at
             .github/profile/README.md.
```

Also observed in the same check:

```
JinnZ2/geometric-to-binary   visibility: PRIVATE
```

§9 says it "currently reads as standalone." A reader following §9 does
not reach a standalone-looking repo — they reach a 404. Not acted on;
recorded.
