# legacy/

Superseded formulations. **Kept, not deleted.**

Per [`../README.md`](../README.md) §10 — *rewrite history is data.* The
subject did not change; the sayable-to-a-model formulation did, as model
capability moved. A deleted earlier formulation destroys that readout.
Successive versions track the receiving instrument, not the subject, and
that is only legible if the versions survive.

Nothing here is wrong-and-discarded. It is **dated-and-superseded**.
Read each file against the constraints in force when it was written
(§1), not against the current page.

## Contents

| file | superseded by | what it records |
|---|---|---|
| `README-front-door.md` | `../README.md` | the front-door text before the reading protocol replaced it. Carries the `calibration-audit` addendum (physics-underneath axiom, layer stack, scope) — still-live content that travelled with the file rather than being split out. |
| `WHY_SO_MANY_REPOS.md` | `../README.md` §1 | the split argued from *inside* the constraint: multiplicity as protection, error-correcting shards, ecological design. §1 now reads the same dispersion as a channel artifact. **Both are dated readings and are not reconciled** — the earlier one is what the constraint looked like from inside it, which is exactly the thing not recoverable from the end state. |
| `political_financial_vectors_v1.py` … `_v9.py` | `../political_financial_vectors_v10.py` | v10's own docstring states the three corrections it makes to v1–v9 (morality-not-substrate, and following). The earlier versions are what the framework looked like before those corrections. Cited as history in `../ALIGNMENT_IMPLICATIONS_GEOMETRY.md`. |

## Naming note

`political_financial_vectors.py` was moved here as
`political_financial_vectors_v1.py`. This is the one rename in the
folder, made because the corpus already refers to that file as `v1`
(`../ALIGNMENT_IMPLICATIONS_GEOMETRY.md`: *"v1 through v8"*). Every other
file keeps its name exactly, per §2.

## Adding to this folder

Move, do not delete. Add a row above stating what supersedes it and what
the earlier formulation records. If a file is superseded because a *gate*
moved rather than because the content was wrong, say so — and put the
gate in [`../gate_log.md`](../gate_log.md).
